<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sirohi.myapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Health Planner"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".Splashscreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Health Planner"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Settings" />
        <activity android:name=".BMICalculator" />
        <activity android:name=".PedoMeter" />
        <activity android:name=".Nearby" />
        <activity android:name=".DietPlan" />
        <activity android:name=".Signup" />
        <activity android:name=".Login" />
        <activity android:name=".Forgot" />
        <activity
            android:name=".nearbysplash"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Health Planner"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".Userinfo" />
        <activity android:name=".Requestoption"></activity>
        <activity android:name="com.example.welcome.bank.MainActivity" />
    </application>

</manifest>